# 🔑 بيانات الدخول المتاحة للاختبار (Test Credentials) — Phase 00

---

## 1. حساب مدير الموارد البشرية (HR Admin)
* **دور الحساب:** `hr`
* **اسم المستخدم:** `admin`
* **البريد الإلكتروني:** `admin@fcpms.test`
* **كلمة المرور:** `password`
* **الحالة:** `active` (نشط)

---

## 2. حساب استشاري ميداني (Field Consultant)
* **دور الحساب:** `consultant`
* **اسم المستخدم:** `consultant`
* **البريد الإلكتروني:** `ahmed@fcpms.test`
* **كلمة المرور:** `password`
* **الحالة:** `active` (نشط)

---

## 3. حساب موقوف لاختبار قواعد العمل (BR-002 Inactive Account)
* **دور الحساب:** `consultant`
* **اسم المستخدم:** `inactive_user`
* **البريد الإلكتروني:** `inactive@fcpms.test`
* **كلمة المرور:** `password`
* **الحالة:** `inactive` (غير نشط - يتم رفض الدخول وإظهار رسالة التوقيف)

---

## 🌐 روابط الصفحة
* **صفحة تسجيل الدخول:** `/login` (`http://localhost:8000/login`)
* **كتالوج مكونات التصميم:** `/design-system` (`http://localhost:8000/design-system`)
